# Part 7 — 記憶體、Clock/Reset、BlackBox（進階）

對應：書 §6.4、§11.4、Appendix A.2 ｜ `import chisel3._`、`import chisel3.util._`

> 進入「會接真實 SRAM macro、多時脈域、vendor IP」的領域。這些是 SoC 整合邊界的必備技巧。

前幾章多半在單一 clock/reset domain 內寫純 Chisel RTL；本章開始碰到真實晶片與 FPGA 專案一定會遇到的邊界：
memory primitive 有固定讀延遲，SRAM macro 來自外部，PLL/pad/vendor IP 不是 Chisel 寫的，
不同子系統可能跑在不同時脈。這些問題不能靠語法糖解決，必須把硬體契約寫清楚。

學完本章後，讀者應該能分辨：何時普通 `Module` 足夠，何時需要 `RawModule`；
`SyncReadMem` 的一拍延遲如何影響 metadata；BlackBox wrapper 要負責哪些 port/clock/reset 契約；
以及為什麼 memory wrapper 比到處直接 instantiate memory primitive 更可維護。

**本章路線圖**

- 先學：`RawModule`、clock/reset scope、CDC 警告、BlackBox、`SyncReadMem`、memory wrapper、RUW。
- 會踩坑：跨時脈直接接、多 bit CDC 不安全、memory data 延遲但 metadata 沒延遲、macro port 對不上。
- 最後能做：把 memory/clock/IP 邊界包成明確 wrapper，並知道哪些行為必須用測試固定。

本章所有 wrapper 都要回答同一組契約問題：

| 邊界 | 必須寫清楚 |
|------|------------|
| clock/reset | 誰提供、是否同步、哪些 register 在哪個 scope |
| memory | read latency、enable semantics、read-under-write policy |
| BlackBox/IP | port 名稱、方向、寬度、latency、side effect |
| CDC | 跨的是單 bit level、pulse，還是多 bit stream |

---

## M7.1 `RawModule`：自己掌控 clock/reset

**一句話**：普通 `Module` 有 implicit clock/reset；`RawModule` 沒有，你自己接。

`Module` 的 implicit clock/reset 很適合大多數同步 RTL，因為它讓內部 register 自動接到同一個時脈域。
但 top-level wrapper、multi-clock subsystem 或 external IP wrapper 常常需要顯式 port。
這時 `RawModule` 讓你從零定義 clock/reset 邊界；代價是你必須自己保證每個 `Reg` 都在正確的
`withClockAndReset` scope 中。

```scala
// 標記：教學骨架。RawModule 需要自行管理所有 clock/reset scope。
class AsyncResetWrapper extends RawModule {
  val clock = IO(Input(Clock()))
  val reset = IO(Input(AsyncReset()))
  val in    = IO(Input(UInt(32.W)))
  val out   = IO(Output(UInt(32.W)))
  withClockAndReset(clock, reset) {       // 在這個區塊內 Reg 才有 clock/reset
    out := RegNext(in)
  }
}
```
適合：top-level wrapper、multi-clock design、blackbox wrapper、ASIC 整合邊界、特殊 reset domain。

---

## M7.2 `withClockAndReset` 與 CDC 警告

`withClockAndReset` 只是在 Chisel elaboration 中改變目前 scope 的 implicit clock/reset，
它不會分析兩個 domain 之間的訊號是否安全。跨 clock 的資料必須經過明確 CDC structure。
教學上可以先從 2-FF synchronizer 處理單 bit control，再進到 `AsyncQueue` 處理多 bit data stream。

```scala
// 標記：概念示意。withClockAndReset 不會自動解決 CDC。
withClockAndReset(cpuClock,  cpuReset)  { val core = Module(new Core) }
withClockAndReset(periClock, periReset) { val uart = Module(new Uart) }
```
> ⚠️ **重要**：Chisel **不會自動幫你做 CDC**。跨時脈域仍需：`AsyncQueue`、synchronizer、pulse crossing、gray-code FIFO、ready-valid CDC bridge。

### CDC 先分成三種問題

CDC 最怕「看起來只是接一條線」。實際上跨 domain 的訊號要先分類，因為不同類型需要不同結構：

| 跨域內容 | 常用結構 | 注意 |
|----------|----------|------|
| 單 bit level control | 2-FF synchronizer | 只能保證 level 被同步，不能保證 pulse 不漏 |
| 單 cycle pulse | pulse stretcher / toggle synchronizer | source pulse 太短時 destination 可能看不到 |
| 多 bit data stream | `AsyncQueue` / async FIFO | data 與 valid 必須一起受 protocol 保護 |

不要把多 bit bus 每一 bit 各接一個 2-FF synchronizer。每一 bit 可能在不同 cycle 穩定，destination
會看到不存在於 source domain 的中間值。若是 address/data/packet，應改用 async FIFO 或明確 handshake。

---

## M7.3 `BlackBox` / `ExtModule`：接外部 Verilog / macro

**一句話**：包 SRAM macro、PLL、vendor IP、pad cell、手寫 Verilog。

BlackBox 的重點是把外部模組變成 Chisel 世界中的 typed boundary。Chisel 不知道外部 Verilog 內部做什麼，
因此 wrapper 必須忠實描述 port 名稱、方向、寬度、clock/reset、enable、mask、latency 與 side effect。
若 wrapper 寫錯，compiler 通常救不了你；錯誤會在模擬連線、綜合或實體整合時才出現。

```scala
// 標記：教學骨架。真實 wrapper 還需要 datasheet 契約與整合測試。
class SramBlackBox extends BlackBox with HasBlackBoxResource {
  val io = IO(new Bundle {
    val clk  = Input(Clock()); val en = Input(Bool()); val wen = Input(Bool())
    val addr = Input(UInt(10.W)); val din = Input(UInt(32.W)); val dout = Output(UInt(32.W))
  })
  addResource("/sram_macro.v")            // 把 .v 一起送進產物
}
```
| 類別 | 差異 |
|------|------|
| `BlackBox` | 有 `val io = IO(new Bundle{...})`，name 對應外部 module |
| `ExtModule` | 直接宣告 port（無 `io` 包裝），更貼近 SystemVerilog |

⚠️ **陷阱**：port 名稱與型別必須與外部 Verilog **完全一致**；clock 顯式 `Input(Clock())`。

---

## M7.4 `SyncReadMem`：對齊讀延遲與 metadata

**一句話**：synchronous-read memory，cycle N 給 addr、cycle N+1 得 data；metadata 要一起延遲。

同步記憶體最常見的 bug 是 data 延遲了，但 addr、tag、way、source id、valid 沒有一起延遲。
Cache、TLB、register file macro、instruction memory 都會遇到同樣問題。設計時可以把 memory read
想成一個一拍 pipeline stage：request metadata 進 register，下一拍和 `rdata` 一起形成 response。

```text
cycle N                       cycle N+1
req.fire ── addr/tag/source ──► reqReg ── tag/source
          └─ mem.read(addr) ────────────► rdata

resp.valid = RegNext(req.fire)
resp.bits  = reqReg metadata + rdata
```

若只延遲 data、不延遲 tag/source，response 會帶著下一筆 request 的 metadata，cache miss、TLB response、
memory client tag 都會因此對錯目標。

```scala
// 標記：教學骨架。重點是 data 和 metadata 同拍對齊。
val ren   = io.req.fire
val raddr = io.req.bits.addr
val rdata = mem.read(raddr, ren)                 // 下一拍才有

val reqReg = RegEnable(io.req.bits, ren)         // ★ metadata 一起延遲對齊
io.resp.valid     := RegNext(ren, false.B)
io.resp.bits.addr := reqReg.addr
io.resp.bits.data := rdata
```
> Cache bug 常來自：tag/data read latency、miss detect latency、request metadata 沒對齊。

---

## M7.5 Memory wrapper：不要到處直接用 `SyncReadMem`

**一句話**：包一層，之後可替換成 BRAM / SRAM macro / sim model / masked-write / ECC 版。

Memory wrapper 是大型專案中非常值得早做的抽象。今天用 `SyncReadMem`，明天可能要換 FPGA BRAM、
ASIC SRAM macro、帶 byte mask 的 macro、ECC memory 或 simulation model。若設計各處直接使用
primitive，替換時會牽動整個 codebase；包成 wrapper 後，使用者只依賴穩定的 read/write 契約。

```scala
// 標記：教學骨架。實務 wrapper 要補 read valid、mask、RUW、init policy。
class Sram1R1W[T <: Data](gen: T, depth: Int) extends Module {
  val aw = log2Ceil(depth)
  val io = IO(new Bundle {
    val r = new Bundle { val en = Input(Bool()); val addr = Input(UInt(aw.W)); val data = Output(gen) }
    val w = new Bundle { val en = Input(Bool()); val addr = Input(UInt(aw.W)); val data = Input(gen) }
  })
  val mem = SyncReadMem(depth, gen)
  when(io.w.en){ mem.write(io.w.addr, io.w.data) }
  io.r.data := mem.read(io.r.addr, io.r.en)
}
```

Wrapper 文件至少要寫清楚：

| 契約 | 要說明什麼 |
|------|------------|
| read latency | `r.en` 後第幾拍 `r.data` 有效 |
| enable semantics | `r.en=false` 時 `r.data` 是否有意義 |
| write mask | 是否支援 byte/bit mask |
| read-under-write | 同位址同拍讀寫回傳舊值、新值或不保證 |
| reset/init | memory 內容是否可預載、reset 後是否定義 |
| target mapping | sim 用 `SyncReadMem`，FPGA/ASIC 是否會替換 macro |

如果 wrapper 介面沒有 `r.valid`，使用者就必須自己在外面延遲 request metadata；如果介面有 `Decoupled`
response，wrapper 內部就應該負責 valid/data 對齊。兩種都可以，但契約不能模糊。

---

## M7.6 Read-under-write policy 要明確

**一句話**：同位址同拍讀寫，各 backend 行為不一致；要 write-first 就自己 bypass。

Read-under-write 不明確是記憶體可攜性的常見陷阱。同一份 Chisel 在不同 backend、FPGA block RAM、
ASIC macro 上可能有不同結果。若設計需要 write-first、read-first 或 no-change 語意，應在 wrapper
中明確實作，並用測試固定下來，不要依賴工具或 macro 的預設行為。

```scala
val raw    = mem.read(raddr, ren)
val bypass = wen && ren && (waddr === raddr)
val rdata  = Mux(bypass, wdata, raw)             // write-first
```

---

## M7.7 Register file 不一定要用 memory

小型 register file 用 `Reg(Vec(...))` 通常最直觀，因為讀取可視為組合 mux，也容易處理 x0 與 bypass。
但 port 數變多、容量變大或 frequency 變高時，成本會迅速上升。這時要考慮 banking、replication、
multi-cycle read、或用 SRAM macro 實作。教學上可以先用 `Reg(Vec)`，但要讓學生知道這不是唯一實作。

```scala
val regs = RegInit(VecInit(Seq.fill(32)(0.U(32.W))))
val rs1  = Mux(io.rs1 === 0.U, 0.U, regs(io.rs1))   // x0 恆 0
val rs2  = Mux(io.rs2 === 0.U, 0.U, regs(io.rs2))
when(io.wen && io.rd =/= 0.U){ regs(io.rd) := io.wdata }
```
小型 CPU 用 `Vec[Reg]` 即可。大型才需考慮：multi-port cost、banking、SRAM macro、bypass、read latency、write-first。

---

## M7.8 ROM / 記憶體預載：`loadMemoryFromFileInline`

程式載入方式應該是平台問題，不應該污染 core 架構。模擬可以用 hex preload，FPGA 可以用 init file，
ASIC 可能有 boot ROM 或外部 memory。把 preload 放進 memory wrapper 或 harness config，
可以讓同一個 core 在不同 target 下重用。

```scala
import chisel3.util.experimental.loadMemoryFromFileInline
val imem = SyncReadMem(depth, UInt(32.W))
loadMemoryFromFileInline(imem, "program.hex")
```
| 階段 | 載入方式 |
|------|----------|
| simulation | preload hex |
| FPGA | init file |
| ASIC | boot ROM macro / 外部記憶體 |

⚠️ **陷阱**：不要把「測試載入方式」綁死進硬體架構；用 wrapper/參數隔離。

---

## M7.9 BlackBox wrapper 應該像 datasheet，而不是只像 port list

接外部 macro 時，`Bundle` 只描述了型別，沒有描述完整行為。真正能讓團隊安全使用 wrapper 的，是把 datasheet
中的重要限制寫進 wrapper 文件、`require`、測試與命名中。尤其 SRAM、PLL、clock gate、pad cell 這類 IP，
錯誤通常不會在 Chisel elaboration 被抓到，而是在 gate-level sim、FPGA build 或實體整合時才爆。

### Wrapper checklist

| 項目 | 例子 |
|------|------|
| Port 對應 | Chisel port 名稱、方向、寬度是否完全對應 Verilog macro |
| Clock/reset | clock edge、reset polarity、是否 async reset |
| Timing | read latency、setup/hold expectation、enable 到 output 的關係 |
| Mask / byte enable | mask bit 與 byte lane 的對應 |
| Undefined behavior | 同位址讀寫、disable 時 output、X-prop 行為 |
| Resource | `addResource`/`addPath` 是否把 Verilog、model、include 一起帶入 |
| Target 差異 | sim model、FPGA primitive、ASIC macro 是否同一個契約 |

### 建議的包法

```scala
case class SramMacroParams(depth: Int, width: Int, readLatency: Int = 1) {
  require(isPow2(depth))
  require(width % 8 == 0)
  val addrBits = log2Ceil(depth)
  val maskBits = width / 8
}

class SramMacro(p: SramMacroParams) extends BlackBox(Map(
  "DEPTH" -> p.depth,
  "WIDTH" -> p.width
)) with HasBlackBoxResource {
  val io = IO(new Bundle {
    val clk  = Input(Clock())
    val en   = Input(Bool())
    val wen  = Input(Bool())
    val mask = Input(UInt(p.maskBits.W))
    val addr = Input(UInt(p.addrBits.W))
    val din  = Input(UInt(p.width.W))
    val dout = Output(UInt(p.width.W))
  })
  addResource("/sram_macro.v")
}
```

上層不應直接依賴這個 raw macro；再包一層 `Sram1R1W` 風格 wrapper，統一 read latency、mask、RUW policy。
這樣日後替換 macro 時，core/cache 不需要知道底層 vendor port 命名。

---

## M7.10 進階記憶體設計判斷

記憶體不是「換成 `SyncReadMem` 就好」。容量、port 數、讀延遲、write mask、clock domain 與 target backend
會一起決定實作方式。

| 需求 | 常見做法 | 代價 |
|------|----------|------|
| 小型、多讀少寫 register file | `Reg(Vec(...))` | 面積隨 mux/port 數快速上升 |
| 中型同步 RAM | `SyncReadMem` | 讀延遲要進 pipeline |
| 多 read port RAM | replication / banking | coherence、write fanout、bank conflict |
| 高容量 SRAM | ASIC macro / FPGA BRAM | wrapper、mask、RUW、初始化要明確 |
| cache tag/data | 分開 memory + metadata 對齊 | miss path 與 bypass 較複雜 |

實作前先問：

1. 讀資料是同拍需要，還是下一拍可以？
2. 同位址讀寫需要定義成 write-first 嗎？
3. 這個 memory 會不會被 FPGA/ASIC flow 替換？
4. request metadata 是否已經跟 data latency 對齊？
5. 測試是否故意打到 read-under-write、back-to-back read、reset/preload 邊界？

**練習**
- ★ 把 Wildcat 的 `imem` 換成 `Sram1R1W` wrapper + `loadMemoryFromFileInline`。完成標準：instruction metadata 與 memory read data 對齊。
- ★★ 為 `Sram1R1W` 加 write-first bypass，ChiselSim 驗證同位址讀寫。完成標準：同拍讀寫同位址時讀值等於新寫入資料。
- ★★★ 寫一個 `RawModule` top + 兩個時脈域，中間用 `AsyncQueue` 過 CDC。完成標準：沒有直接把多 bit data 從一個 clock domain 接到另一個 domain。
